<h2>会員一覧</h2>

会員ID
氏名
メールアドレス
ステータス

<% @customers.each do |customer| %>

<%= customer.id %>

<%= limk to customer.last_name customer.first_name, admin_customer_path(customer)  %>

<%= customer.email %>

<% if customer.is_deleted == true %>
  退会
<% else %>
  有効
<% end %>